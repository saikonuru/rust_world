// Module: maths
// * Define a module named maths
pub mod maths {
    // ** must be public method to be accessed from outside
    pub fn addition(a: i32, b: i32) -> i32 {
        a + b
    }
    pub fn subtraction(a: i32, b: i32) -> i32 {
        a - b
    }
}
